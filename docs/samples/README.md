# Sample assets

Place exported backend outputs in subfolders here if you want to host them with the static frontend.

Example:

samples/lesson-01/audio.mp3
samples/lesson-01/cleaned.txt
samples/lesson-01/cleaned.json
samples/lesson-01/cleaned.srt
samples/lesson-01/cleaned.vtt

Then register those paths in `src/data/samples.ts`.
